@page "/custom-domain"

<MudText Typo="Typo.h4">Custom Domain</MudText>
<MudText>1. Add CNAME record: <code>mydomain.com → linkshub.me</code></MudText>
<MudText>2. Wait for SSL (auto-managed by Render).</MudText>